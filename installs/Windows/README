To run revice in cmd just run godownload.exe
To run hiden user Start.bat

**BUG**
save setting don't work when you kill application.
if you use cntrl+c in cmd mode save settigs work normal.
It happen in difference realizayion kill procees linux and Windows
in linux OS kill process work normal. App capture signal and do some cleanup stuff

    gdownsrv := new(DownloadService.DServ)
	c := make(chan os.Signal, 1)
	signal.Notify(c, os.Interrupt)
	signal.Notify(c, syscall.SIGTERM)
	go func() {
		<-c
		func() {
			gdownsrv.StopAllTask()
			log.Println("info: save setting ", gdownsrv.SaveSettings(getSetPath()))
		}()
		os.Exit(1)
	}()